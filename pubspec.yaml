name: prysm_video_player
description: A premium cross-platform Flutter video player built on media_kit with responsive controls and a simple API.
version: 0.0.1
homepage: https://github.com/prysm/prysm_video_player

environment:
  sdk: ^3.12.0
  flutter: ">=3.16.0"

dependencies:
  crypto: ^3.0.7
  flutter:
    sdk: flutter
  http: ^1.6.0
  media_kit: ^1.2.6
  media_kit_libs_video: ^1.0.7
  media_kit_video: ^2.0.1
  path_provider: ^2.1.5

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
